Gradle build for CPJ Java helper libraries

Usage:

- Build:

    ./gradlew build

- Create a runnable jar:

    ./gradlew jar

Notes:
- This is a minimal Gradle configuration to manage Jackson and other Java deps.
- If you prefer Maven, I can add a `pom.xml` instead.
